<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 设置填充颜色 -->
    <solid android:color="@color/btn_bg_color_1" />

    <!-- 设置圆角矩形 -->
    <corners
        android:bottomLeftRadius="@dimen/btn_radius_1"
        android:bottomRightRadius="@dimen/btn_radius_1"
        android:topLeftRadius="@dimen/btn_radius_2"
        android:topRightRadius="@dimen/btn_radius_1" />

    <!-- 设置边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />

</shape>