<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 设置填充颜色 -->
    <solid android:color="@color/switch_nei_1" />

    <!-- 设置圆角矩形 -->
    <corners
        android:bottomLeftRadius="@dimen/switch_radius_1"
        android:topLeftRadius="@dimen/switch_radius_1" />

    <size
        android:width="@dimen/switch_width_2"
        android:height="@dimen/switch_height_1" />

    <stroke
        android:width="1dp"
        android:color="@color/black" />
</shape>